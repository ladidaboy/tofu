package cn.hl.ax.spring.base.bean;

/**
 * Query Object 查询参数对象
 * <ul>
 * <li>*控制层(Web层) / *接口层(Service层) -> 业务层(Biz层) -> 持久层(Dao层)  查询数据时使用的参数封装对象</li>
 * <li>类似于 RO 的一个子集，但并不拘泥于控制层(Web层)</li>
 * </ul>
 * @author hyman
 * @date 2019-12-30 03:46:51
 */
public interface QO {
}
